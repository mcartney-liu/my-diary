# MyDiary 版本更新记录

## v0.5.0 — 2026-09-21

### 🎉 新功能

- **🧠 知识库系统（完整版）** — AI 帮你把零散的资料整理成一套相互关联、便于查找的笔记
  - 多知识库管理：一个账号可以建多个独立的知识库（比如"三国历史"、"宇宙大爆炸"）
  - **Obsidian 风格双向链接**：页面正文里用 `[[页面名]]` 互相引用，点击直接跳转；找不到的链接不会报错，会提示新实体
  - **知识图谱**：自动生成力导向图，节点可拖拽、滚轮缩放、点击聚焦，连边越多的节点越大
  - **反链上下文**：打开一个页面，底部显示「谁提到了我」+ 那段原文摘要（纯文本匹配，不花 AI）
  - **页面可编辑**：点 ✏️ 进入编辑模式，改完保存自动识别新增的 `[[新实体]]`，可选分类后一键生成完整页面
  - **重复汇入自动合并**：同一资料再次汇入不会产生重复页面，AI 会把新信息整合进已有页面
- **📚 全局资料库** — 从知识库里独立出来，所有原始资料统一管理
  - 支持粘贴文本 / 上传文件（.txt .md .docx .csv .html .json）
  - KB 标签式绑定：一篇资料可以打多个知识库的标签，汇入哪个由你决定
  - 批量汇入：选中多条资料 → 选目标知识库 → 一次性汇入，完成后显示新建/更新/链接数量
  - 汇入时自动给资料打 KB 标签（后续可快速筛选）
- **📋 范本独立管理** — 范本从分类里抽出来，作为单独 tab 管理
  - 6 个官方范本（人物/事件/地点/日记/模板/通用），全部中文化占位符（`{{身份}}`、`{{参与人}}`）
  - 自定义范本：自己创建、删除，分类直接引用范本库
- **🏠 首页多了「🧠 知识」入口** — 和月历 / 年度回顾 / 小麦并列
  - 直接复用知识库组件，只看结果（树形层级 + 图谱）
  - 带引导文字：告诉用户要去「我的」里配置

### 🔧 交互改进

- **知识库配置 UI 重排** — 更符合操作习惯
  - KB selector 无框下拉（默认态不出现编辑暗示），只有点「修改」才变成可编辑输入框
  - 按钮布局重新调整：`+ 新建` 独占左列，`修改 / 删除` 竖排右列，高度一致
  - 分类编辑器表单：**名称 → 套用范本 → 格式预览**（加了实时 Markdown 预览区）
- **范本 tab 响应式** — 4 个 tab 挤不下时横向滚动，手机不换行
- **资料列表** — 手动添加时填了标题就显示标题，不显示"手动文本"
- **「我的」tab 去掉「库」字** — `模板库 / 信纸库 / 知识库` → `模板 / 信纸 / 知识`

### 🐛 Bug 修复

- **知识页面标题后面多出一个 "0"** — `is_system` 字段值是 `0`（不是 boolean），前端 JSX 里 `{xxx && ...}` 把 `0` 渲染成了字符串 "0"，改成严格比较
- **反链上下文显示不出来** — 后端批量拉页面时截断 content 为前 200 字，目标标题往往出现在正文中间；加上历史页面 content 里没有 `[[xxx]]` 语法；两层问题叠加
- **资料添加报 500** — `wiki_sources.kb_id` 字段原来不允许为空，改成全局化后添加资料不强制选 KB，传了 null 触发约束冲突；重建表去掉 NOT NULL
- **历史页面 content 里有重复的大标题** — 之前合并是 `旧内容 + AI 新吐的完整 markdown`，AI 每次吐的完整 markdown 又带一个同名 `# 标题`；改成 AI 返回整合后的完整内容，直接替换
- **汇入不更新「目录.md」和「日志.md」** — 之前后端硬编码拼，现在用 AI 返回的 summary 和 log_entry；目录条目带一句话说明

### 🏗️ 后端架构

- **汇入流程升级**：prompt 里喂完整的已有页面（之前只喂目录.md），AI 返回 `action: "update" | "create"`，后端按此替换或新建
- **DB Schema**：新增 migration 0010~0015（wiki 相关表、范本、summary 字段、tags 字段、重建 wiki_sources 去掉 kb_id NOT NULL）

### 📦 本次改动文件

| 文件 | 改动 |
|------|------|
| `src/components/KnowledgeBase.tsx` | **全新** — 知识库主组件（配置/范本/页面/图谱 + 编辑/生成实体） |
| `src/components/SourcesPanel.tsx` | **全新** — 全局资料库面板（列表/筛选/批量汇入/文件上传） |
| `workers/src/wiki.js` | **全新** — 知识库核心逻辑（CRUD + AI 汇入 + 全局资料 API） |
| `workers/src/index.js` | +4 全局资料路由 + +10 wiki 路由 |
| `workers/migrations/0010~0015_*.sql` | 6 个数据库迁移 |
| `src/api.ts` | +20 个 wiki API 函数 |
| `src/components/CalendarPage.tsx` | +「🧠 知识」tab 入口 |
| `src/components/ProfilePage.tsx` | +「📚 资料」平级入口 |
| `src/App.tsx` | + `/sources` 路由 |
| `vite.config.ts` | `0.4.1` → `0.5.0` |
| `package.json` | `0.3.1` → `0.5.0` |

---

## v0.4.1 — 2026-09-20

### 🎉 新功能

- **💬 日记知识库改名为「小麦」** — v0.4.0 新增的「日记知识库」正式更名为「💬 小麦」，多会话管理 / 历史抽屉 / 自动保存全部保留，功能不变
- **🧠 小麦会记住你了 — 长期记忆系统**
  - 聊天时提到自己的事（"我讨厌加班"），小麦会自动提取成记忆存下来
  - 下次聊到相关的自动用上，不用重复说
  - 回答结尾偶尔说"我会记住这点的～"，然后真的记下来了
  - 「我的」页面新增 **🧠 记忆** tab：手动加、看全部、点一下删除
  - 五种记忆类型：💫 偏好 / 📋 个人 / 📌 事实 / ✅ 待办 / 🎨 兴趣
- **🔍 回答更准了 — 检索三层升级**
  - **混合检索**：向量相似度（0.7）+ 关键词匹配（0.3）加权融合。之前"还了 3000 信用卡"会被"花了 3000 吃饭"干扰，现在精确命中
  - **ReRank 精排**：bge-reranker-base 对初筛 top12 条候选专家精选 top5，喂给 LLM 的每条都真正相关
  - **常识问题自动跳过**："中国面积多大"直接让 LLM 自由发挥，不查日记

### 🐛 Bug 修复

- **Agnes LLM 偶尔返回乱码** — BOM 头没剥干净，修了
- **关键词路由不准** — "我想加班"里的"我想"被误判成日记关键词，修了
- **边界情况请求直接炸** — handleAsk try-catch 没闭合，修了

### 📦 本次改动文件

| 文件 | 改动 |
|------|------|
| workers/migrations/0009_diary_embeddings.sql | 新增 diary_embeddings 表（向量存储） |
| workers/src/index.js | 混合检索 + ReRank + 长期记忆 CRUD + 关键词路由 + BOM strip |
| src/components/ProfilePage.tsx | 新增「🧠 记忆」tab + v0.4.1 版本更新弹窗 |
| src/components/AiChatView.tsx | 改名为「💬 小麦」+ 空状态文案 + 记忆提取集成 |
| src/components/CalendarPage.tsx | 顶栏 Tab「知识库」→「💬 小麦」 |
| src/api.ts | 新增 listMemory / addMemory / deleteMemory 接口 |
| vite.config.ts | 版本 0.4.0 → 0.4.1 |

---

## v0.4.0 — 2026-09-19

### 🎉 新功能

- **💬 日记知识库** — 首页顶部 Tab 新增「💬 知识库」入口，和月历 / 年度回顾并列
  - 能从所有日记里按语义找片段回答问题，附带引用来源
  - 多会话管理：点左上角 ☰ 打开历史抽屉，「＋ 新对话」新建空白，点历史项切换回去，鼠标悬停可删除单个会话
  - 聊天记录自动保存，刷新页面、重新登录都不丢
  - 问候不再死板："你好"、"你是谁" 不再说"没找到相关内容"，会友好介绍自己能帮什么

### 🧭 交互调整

- 顶栏导航变成三 Tab（📅 月历 / 📊 年度回顾 / 💬 知识库），AI 入口从底部导航移除，保持底部三项不变
- 知识库内嵌在 Tab 切换区，不再是右侧抽屉，和月历 / 年度回顾同级同宽

### 🏗️ 后端架构

- Workers AI bge-m3 embedding + cosine similarity 检索
- Agnes LLM（优先）+ Workers AI llama fallback
- D1 BLOB 存向量，reindex 全量重算

### 📦 本次改动文件

| 文件 | 改动 |
|------|------|
| workers/src/index.js | AI 问答核心：embedText / cosineSimilarity / handleAsk / extractDiaryText |
| workers/wrangler.toml | 新增 `[ai] binding = "AI"` 绑定 Workers AI |
| src/components/AiChatView.tsx | 全新 — 知识库聊天组件 |
| src/components/CalendarPage.tsx | 顶栏新增知识库 Tab + 条件渲染 |
| src/api.ts | 新增 askDiary / getSessions / createSession 等接口 |
| src/types.ts | 新增 ChatSession / ChatMessage 类型 |
| vite.config.ts | 版本 0.3.1 → 0.4.0 |

---

## v0.3.1 — 2026-09-18

### 🎉 新功能

- **📊 记账统计页面** — 顶栏多了 📊 入口，点进去能看周/月汇总
  - 每天收入、支出、结余一目了然
  - 柱图：每天一根柱子，红色支出在上、绿色收入在下，从右往左生长
  - 分类前几名：自动找出钱主要花在哪几类
- **🖼️ 图片能拖放调大小了** — 写日记时图片右下角有个小圆点
  - 鼠标悬停或长按才出现，拖一下就能缩小放大（25% 到 150%）
  - 手机触屏也能用，拖的时候会显示实时百分比
- **🖼️ 图片上传自动压缩** — 传大图也不怕吃配额
- **📅 日历顶栏加了 📊 入口** — 一键跳到记账统计页
- **🎠 顶栏导航跑马灯** — 手机端图标多了挤不下，自动左右循环滑（像跑马灯一样）
  - 手指按住能停住，拖一下能手动滑动
  - 松手后继续自动循环，始终在顶栏那条区域里

### 🐛 Bug 修复

- **📊 柱图方向反了 + 日期显示不全** — 之前柱子从左往右长不符合直觉；日期轴还只显示偶数天，日期多了看不全；都改了
- **📈 每日一句话总说"今天没什么特别的"** — 之前只看文字内容，记账模板全是流水没有文字，AI 每次都兜底；现在能识别收支流水了
- **📖 历史抽屉显示 0 条** — 后端过滤条件问题，修了
- **🔙 从列表页点进日记，返回键跳回首页** — 之前硬编码跳首页，现在回到上一页（不管是记账统计、纪念日还是计划）

### 🔧 交互改进

- **新建空记账模板会拦住你** — 改了标题但没填任何收支就点保存 → 弹窗提示"至少记一笔再保存"

### 🛡️ 重复日记彻底解决

之前偶尔会出现"同一篇日记存了两遍"，原因是每次保存都生成新的唯一标识。现在三层拦截：

1. 后端：同日期 + 同模板 + 同标题（记账/纪念日/计划）→ 直接更新旧的，不新建
2. 前端：收到后端返回的最终标识，自动把本地纠正过来
3. 启动时：本地缓存和云端合并 → 只留最新那条

### 📦 本次改动文件

| 文件 | 改动 |
|------|------|
| src/components/FinancePage.tsx | 全新 — 记账统计独立页面 |
| src/categories.ts | 全新 — 收支分类体系 |
| src/components/ImageBlock.tsx | 图片拖放调大小 + 自动压缩 |
| src/App.tsx | 重复日记去重 + 返回键回上一页 |
| src/components/EditorPage.tsx | 空模板拦截 |
| src/components/CalendarPage.tsx | 顶栏 📊 入口 + 跑马灯 |
| src/components/ProfilePage.tsx | 版本更新记录弹窗 |
| src/ai.ts | 每日一句话识别收支流水 |
| src/api.ts | 每日总结接口 |
| src/index.css | 跑马灯 keyframes |
| workers/src/index.js | 后端同标题自动合并更新 |
| workers/migrations/0008_diaries_add_cols.sql | 新增数据库迁移 |
| CHANGELOG.md | v0.3.1 版本记录 |
| DEPLOY.md | 写作风格规范 |
| package.json / vite.config.ts | 版本 0.3.0 → 0.3.1 |

---

## v0.3.0 — 2026-09-17

### 🎉 新功能

- **🎯 计划模块（原每日计划升级）** — 从「每日计划」模板扩展成完整的计划系统，不再局限于当天
  - 任何未来日期的计划都能写成日记，自动识别目标日期、倒计时、到期状态
  - 顶部导航栏 🎯 入口 + 完整 PlanPage/PlanLibrary 组件（和纪念日/时间胶囊并列）
  - 后端 plans 子表双写，和日记关联但有独立的状态管理
- **📖 每日一句话入库 + 历史查看** — 首页 StreakBadge 的一句话不再只是临时缓存，每天自动提炼的一句话存起来，点「📖 历史」弹出抽屉查看
  - 后端 daily_summaries 表 + Worker CRUD
  - 前端 StreakBadge 自动调用 AI 总结 + 持久化

### 🐛 Bug 修复

- **📈 每日一句话空白** — 首页不显示总结内容，修了
- **🎯 计划日期识别不准** — 模糊日期（"下周一"）误判

### 🔧 交互改进

- **新建空模板引导** — 日记列表为空时显示友好引导，教用户怎么开始

### 📦 本次改动文件

| 文件 | 改动 |
|------|------|
| workers/migrations/0006_plans.sql | 新增 plans 子表 |
| workers/migrations/0007_daily_summaries.sql | 新增 daily_summaries 表 |
| workers/src/index.js | plans CRUD + summaries CRUD |
| src/components/PlanLibrary.tsx | 全新 — 计划列表组件 |
| src/components/PlanPage.tsx | 全新 — 计划独立页面 |
| src/components/CalendarPage.tsx | 顶栏加 🎯 Plan 入口 |
| src/types.ts | Diary 加 plan_info |
| src/api.ts | Plan 类型 + savePlan + DailySummary |
| src/ai.ts | summarizeDay 完善 |
| src/components/StreakBadge.tsx | 每日一句话持久化 + 历史抽屉 |
| CHANGELOG.md | v0.3.0 版本记录 |
| package.json / vite.config.ts | 版本 0.2.1 → 0.3.0 |

---

## v0.2.1 — 2026-09-16

### 🎉 新功能

- **💾 日记 Block 软删除** — 删除后划掉显示 + 恢复按钮，二次确认后才真正删除（修了很久的核心交互）
- **✍️ 继续书写** — 编辑器底部直接写新内容，不用点按钮新建 block
- **📚 模板/信纸共享系统** — 共享给所有人、带作者信息
- **🖋️ 字体自定义** — 「我的 → 设置 → 字体样式」三种可选
  - ✍️ 全手写（推荐）：中文钢笔 + 英文手写
  - 🖋️ 钢笔中文：中文手写、英文印刷
  - 📝 系统默认：简洁印刷体
  - 设置自动保存，下次打开自动恢复
  - 全局生效：标题、按钮、日历、编辑器文字全部跟着变
- **🧪 Dev Pages 测试环境** — mydiary-web-dev.pages.dev，永久不变，前端自动连对应后端

### 🐛 Bug 修复

- **删除后恢复功能** — 软删除后能恢复的核心交互 bug
- **iOS Safari 中文字体不手写** — WebKit 的字体加载 bug，通过强制重排修复
- **Tailwind 覆盖手写字体** — Tailwind 给 html 注入了印刷体，现在 html 和 body 同时设手写体
- **Kalam 放字体栈第一位** — 之前顺序反了导致英文数字回退到印刷楷体

### 📚 运维文档

- **DEPLOY.md 重写** — 完整的双环境（测试/生产）架构、部署流程、回滚方案、命令速查

### 📦 本次改动文件

| 文件 | 改动 |
|------|------|
| src/components/ProfilePage.tsx | 字体设置 + 版本更新记录弹窗 |
| src/components/EditorPage.tsx | Block 软删除 + 底部续写 |
| src/index.css | Tailwind 覆盖 + iOS 重绘修复 |
| src/main.tsx | 启动时恢复字体设置 + iOS 重绘触发 |
| DEPLOY.md | 完整运维手册 |
| CHANGELOG.md | v0.2.1 版本记录 |
| package.json / vite.config.ts | 版本 0.2.0 → 0.2.1 |

---

## v0.2.0 — 2026-09-16

### 🎉 新功能

- **📚 模板共享系统** — 创建自定义模板、一键共享给所有人
  - 👤 我的模板库：创建 / 编辑 / 删除 / 共享
  - 🌐 共享模板：在日记模板抽屉里看别人发布的模板
  - 模板 Builder：选组件组合（标题/待办/记账/摘抄...）+ 业务化描述
- **🎨 信纸共享系统** — 上传自己的信纸、一键共享
  - 👤 我的信纸库：上传 / 删除 / 共享
  - 🌐 共享信纸：在日记信纸抽屉里看别人的信纸
- **📝 反馈与建议** — 「我的 → 关于 → 反馈」直接提交到数据库
- **👤 全新「我的」页面** — 用户卡片 + 写作统计 + 模板库 + 信纸库 + 设置 + 关于

### 🔧 交互改进

- 模板/信纸抽屉选中色统一 **紫色**
- 模板切换：有内容时弹"切换确认"
- 删掉抽屉里的编辑/删除按钮 → 统一去「我的」页面管理
- 删掉信纸抽屉里的「自定义/从相册选」→ 统一去「我的 → 信纸库」上传

### 🏗️ 架构

- **双环境隔离**：测试 Pages + 生产 Pages 各自代理到对应后端
- **Workers + D1**：后端全部 Cloudflare 无服务器
- **Pages Functions**：同域代理接口请求，绕开 workers.dev 拦截

---

## v0.1.0 — 2026-09-10

### 基础框架

- 邮箱注册/登录 + JWT 鉴权
- 日记 CRUD + 本地缓存 ↔ D1 双向同步
- 10 种 Block 组件：文本 / 标题 / 分割线 / 待办 / 数字 / 记账 / 图片 / 音频 / 读书 / 摘抄
- 8 个官方模板：日记 / 记账 / 读书 / 旅行 / 运动 / 每日计划 / 感恩日记 / 健康记录
- 底部 Tab Bar：我的 | AI速记 | 写日记
